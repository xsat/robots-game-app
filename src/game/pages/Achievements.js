import React from "react";
import {Common} from "../components";

export class Achievements extends Common{
    render() {
        return (
            <React.Fragment>
                <h1>Achievements</h1>
            </React.Fragment>
        );
    }
}